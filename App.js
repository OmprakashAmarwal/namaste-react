const header = React.createElement("h1",{id:"header"},"My First React Program in React");
console.log(header);
const root = ReactDOM.createRoot(document.getElementById("root"));
console.log(root);

root.render(header);